import { UIApiTable, ClassRenderPropTable } from "@lynx-ui/index";
import { useLang } from '@rspress/core/runtime';

export const ClassRenderPropPreamble = () => {
  const isZh = useLang() === 'zh';
  if (isZh) {
    return (
      <>
        <h3>基于状态的样式与渲染</h3>
        <p>
          该组件将每一项内部状态通过<strong>两条等价通道</strong>同时对外暴露，下表逐行给出两者的对应关系：
        </p>
        <ul>
          <li>
            <strong>CSS className</strong>：根节点上会动态加上 <code>{'ui-<state>'}</code> 形式的类名，可直接用 <code>{'.ui-<state> { ... }'}</code> 选择器定制样式；
          </li>
          <li>
            <strong>Render-prop 字段</strong>：当 <code>children</code> 传入函数时，同一个状态会以 <code>{'children({ <state>: boolean, ... })'}</code> 的形式作为参数传入，便于在运行时做条件渲染。
          </li>
        </ul>
      </>
    );
  }
  return (
    <>
      <h3>State-based styling & rendering</h3>
      <p>
        This component exposes each of its internal states through <strong>two interchangeable channels</strong>, and the table below maps them one-to-one:
      </p>
      <ul>
        <li>
          <strong>CSS className</strong> — a class like <code>{'ui-<state>'}</code> is toggled on the root node, so you can style it with selectors such as <code>{'.ui-<state> { ... }'}</code>.
        </li>
        <li>
          <strong>Render-prop field</strong> — when <code>children</code> is a function, the same state is passed in via <code>{'children({ <state>: boolean, ... })'}</code> for runtime-driven rendering.
        </li>
      </ul>
    </>
  );
};

  

### Checkbox 
    
  
<UIApiTable source={[
  {
    "name": "checkboxProps",
    "type": "ViewProps",
    "summary": [
      {
        "kind": "text",
        "text": "Checkbox supports original view props to be directly spread in this prop."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "Checkbox 支持将原始视图属性直接展开到这个属性中。"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "checked",
    "type": "boolean",
    "summary": [
      {
        "kind": "text",
        "text": "Whether the Checkbox is checked. If this property is set, the Checkbox will be in controlled mode, meaning the defaultChecked property will take no effects."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "是否选中。如果设置了此属性，Checkbox 将处于受控模式，这意味着 defaultChecked 属性将不起作用。"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "className",
    "type": "string",
    "summary": [
      {
        "kind": "text",
        "text": "className"
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "类名"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": true,
    "docTypeFallback": null
  },
  {
    "name": "defaultChecked",
    "type": "boolean",
    "summary": [
      {
        "kind": "text",
        "text": "Determines whether the Checkbox is checked by default. Use this property means the Checkbox is uncontrolled."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "默认是否选中。使用此属性意味着 Checkbox 为非受控状态。"
      }
    ],
    "defaultValue": "false",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "disabled",
    "type": "boolean",
    "summary": [
      {
        "kind": "text",
        "text": "Disables the Checkbox. The Checkbox cannot be interacted with."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "是否禁用。如果设置了此属性，Checkbox 将无法交互。"
      }
    ],
    "defaultValue": "false",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "indeterminate",
    "type": "boolean",
    "summary": [
      {
        "kind": "text",
        "text": "The indeterminate state of the Checkbox."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "是否为不确定状态。"
      }
    ],
    "defaultValue": "false",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "onChange",
    "type": "(checked: boolean) => void",
    "summary": [
      {
        "kind": "text",
        "text": "The callback function that is triggered when the state changes"
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "状态变化时触发的回调函数"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "style",
    "type": "CSSProperties",
    "summary": [
      {
        "kind": "text",
        "text": "style"
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "样式"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": true,
    "docTypeFallback": null
  }
]}/>
      
<UIApiTable source={[
  {
    "name": "children",
    "type": "ReactNode | [object Object]",
    "summary": [
      {
        "kind": "text",
        "text": "children"
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "子节点"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": {
      "tag": "@docTypeFallback",
      "content": [
        {
          "kind": "text",
          "text": "ReactNode | ((status: { checked: boolean; indeterminate: boolean; active: boolean; disabled: boolean }) => ReactNode)"
        }
      ]
    }
  }
]}/>
<ClassRenderPropPreamble />
<ClassRenderPropTable source={[
  {
    "className": "ui-checked",
    "Render Prop": "checked",
    "item": {
      "type": "boolean",
      "description": "Whether the checkbox is currently checked.\nApplied when `status.checked` is true.",
      "description_zh": "复选框当前是否处于选中态。\n当 status.checked 为 true 时生效，可用于 `.ui-checked { ... }`。"
    }
  },
  {
    "className": "ui-indeterminate",
    "Render Prop": "indeterminate",
    "item": {
      "type": "boolean",
      "description": "Whether the checkbox is in an indeterminate state.\nApplied when `status.indeterminate` is true.",
      "description_zh": "复选框是否处于不确定态。\n当 status.indeterminate 为 true 时生效，可用于 `.ui-indeterminate { ... }`。"
    }
  },
  {
    "className": "ui-active",
    "Render Prop": "active",
    "item": {
      "type": "boolean",
      "description": "Whether the checkbox is currently being pressed (and not disabled).\nApplied when `status.active` is true.",
      "description_zh": "复选框当前是否处于按下态（且未被禁用）。\n当 status.active 为 true 时生效，可用于 `.ui-active { ... }`。"
    }
  },
  {
    "className": "ui-disabled",
    "Render Prop": "disabled",
    "item": {
      "type": "boolean",
      "description": "Whether the checkbox is disabled.\nApplied when `status.disabled` is true.",
      "description_zh": "复选框是否处于禁用态。\n当 status.disabled 为 true 时生效，可用于 `.ui-disabled { ... }`。"
    }
  }
]}/>
      

### CheckboxIndicator 
    
  
<UIApiTable source={[
  {
    "name": "children",
    "type": "ReactNode",
    "summary": [
      {
        "kind": "text",
        "text": "The indicator of the Checkbox. Only displays child nodes when checked or indeterminate is true. If you need to keep it displayed when checked or indeterminate is false, set forceMount to true."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "Checkbox 的提示框，仅当 checked 或 indeterminate 为 true 时显示子节点。如果需要在 checked 或 indeterminate 为 false 时也保持子节点的显示，需要将 forceMount 设置为 true。"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "className",
    "type": "string",
    "summary": [
      {
        "kind": "text",
        "text": "className"
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "类名"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": true,
    "docTypeFallback": null
  },
  {
    "name": "forceMount",
    "type": "boolean",
    "summary": [
      {
        "kind": "text",
        "text": "Force mount the children. If set to true, the children will always be mounted even when checked or indeterminate is false."
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "强制挂载子节点。如果设置为 true，即使 checked 或 indeterminate 为 false，子节点也会被挂载。"
      }
    ],
    "defaultValue": "false",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": false,
    "docTypeFallback": null
  },
  {
    "name": "style",
    "type": "CSSProperties",
    "summary": [
      {
        "kind": "text",
        "text": "style"
      }
    ],
    "summary_zh": [
      {
        "kind": "text",
        "text": "样式"
      }
    ],
    "defaultValue": "",
    "isOption": true,
    "isSupportIOS": true,
    "isSupportAndroid": true,
    "isSupportHarmony": true,
    "docTypeFallback": null
  }
]}/>
      

  