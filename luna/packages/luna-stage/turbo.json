{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@lynx-js/luna-core#build"],
      "inputs": [
        "src/**",
        "tsconfig.build.json",
        "rslib.config.ts"
      ],
      "outputs": ["dist/**"]
    }
  }
}
