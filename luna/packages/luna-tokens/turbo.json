{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@lynx-js/luna-core#build"],
      "inputs": [
        "package.json",
        "src/**",
        "rslib.config.ts",
        "tsconfig.build.json",
        "../../tsconfig.json"
      ],
      "outputs": ["dist/**"]
    }
  }
}
