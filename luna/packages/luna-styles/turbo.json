{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputLogs": "full",
      "dependsOn": ["^build"],
      "inputs": [
        "package.json",
        "src/**",
        "scripts/**",
        "rslib.config.ts",
        "tsconfig.json",
        "../../tsconfig.json"
      ],
      "outputs": ["dist/**"]
    }
  }
}
