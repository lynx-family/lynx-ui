---
'@lynx-js/lynx-ui-swiper': patch
---

Default the Swiper root view's `z-index` to `0` so z-indexed items remain in the wrapper event chain and directional gesture consumption works in custom mode.
