---
'@lynx-js/lynx-ui-sheet': minor
'@lynx-js/lynx-ui': minor
---

Add `Sheet` side support with `top`, physical `left` / `right`, logical `start` / `end`, width-based drawer snap points, `enableRTL` logical-side resolution, and directional examples.
